{
    "name": "demo-auth-jsonserver",
    "version": "1.0.0",
    "scripts": {
        "server": "json-server-auth db.json --port 3000"
    },
    "devDependencies": {
        "json-server": "0.17.4",
        "json-server-auth": "^2.1.0"
    }
}
